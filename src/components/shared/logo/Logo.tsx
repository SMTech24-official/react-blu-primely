import logo from "@/assets/logo/logo.png"
const Logo = () => {
    return (
        <div>
            <img src={logo} alt='Brand Logo' width={100} height={100} className='w-[80px] h-[74px]' />
        </div>
    );
};

export default Logo;